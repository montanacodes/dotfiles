Using Cram.com with - for flip card and | for next card

JavaScript variables are-containers for data values|

Declaring a variable-const x = 5 (constant)
let y = 6 (changable)
var z = 7 (defunct)|

for loop-
for (expression 1; expression 2; expression 3) {
  // code block to be executed
}

for (let i = 1; i <= 5; i++) {
    console.log(i);
}|

WHILE LOOP-while (condition) {
  // code block to be executed
}
var i = 1;
while (i < 6) {
    console.log(i);
    i++;
};
console.log('Countdown finished!')|

ARRAY LITERAL SYNTAX-var train1 = ["wheat", "barley", "potato", "rocks"]|

Function SYNTAX-function name(parameter1, parameter2, parameter3) {
  // code to be executed
}
|
Function parameters are listed- inside the parentheses () in the function definition.|

Function arguments are- the values received by the function when it is invoked.|

Calling a function-let x = myFunction(4, 3);   
// Function is called, return value will end up in x

function myFunction(a, b) {
  return a * b;             
}
// Function returns the product of a and b|

an object consists of key-value pairs, known as -properties.​|